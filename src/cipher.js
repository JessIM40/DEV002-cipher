const cipher = {
// cipher.encode(offset, string)
// cipher.decode(offset, string)

  // funcion para encriptar

    // Ciclo para recorrer por cada caracter
      // Reemplazar letra por letraCifrada
        // Obtener codigo ASCII
        // Obtener desplazamiento elegido por usuario


  // Mostrar resultado
  // Limpiar texto de textarea


};

export default cipher;
